version: '3.8'

services:
  mysql:
    image: mysql:8.4
    container_name: techflow-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: rootpassword
      MYSQL_DATABASE: techflow_erp
      MYSQL_USER: techflow_user
      MYSQL_PASSWORD: techflow123
    ports:
      - "3307:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    networks:
      - techflow-network

  adminer:
    image: adminer:latest
    container_name: techflow-adminer
    restart: always
    ports:
      - "8081:8080"
    networks:
      - techflow-network
    depends_on:
      - mysql

  backend:
    build: ./backend
    container_name: techflow-backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://mysql:3306/techflow_erp?allowPublicKeyRetrieval=true&useSSL=false
      - SPRING_DATASOURCE_USERNAME=techflow_user
      - SPRING_DATASOURCE_PASSWORD=techflow123
      - SPRING_PROFILES_ACTIVE=dev
    volumes:
      - ./backend:/app
      - ~/.m2:/root/.m2
    depends_on:
      - mysql
    networks:
      - techflow-network

  frontend:
    build: ./frontend
    container_name: techflow-frontend
    ports:
      - "4200:4200"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - techflow-network
    depends_on:
      - backend

volumes:
  mysql_data:

networks:
  techflow-network:
    driver: bridge